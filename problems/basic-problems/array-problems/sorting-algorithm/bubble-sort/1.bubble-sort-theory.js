/**
 * @author Jenny's Lecture
 * Basic principal of bubble sort: Two adjecent element are to be compared.
 * if those element are in correct order then it's fine will move fruther and  if those element 
 *  are not in correct order  then will swap the element.
 */

/**
 * A[i] is compared with A[i+1], if these element are in corect position no swapping wii be done if not then, swappig will done.
 * if number of element are 'n' in an array then 'n-1' passes are required to sort the array. (in general)
 */