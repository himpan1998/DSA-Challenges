/**
 * Find the element that appears the most times in the array.
 *Input: arr=[1, 2, 3, 4, 5, 3, 2, 2];
 * Output:2
 */

/**
 * Finds the element that appears the most times in the array.
 *
 * @param {Array<number>} arr - The array of numbers to analyze.
 * @returns {number} - The element that appears the most times in the array.
 */
